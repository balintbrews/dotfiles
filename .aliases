## Frequently used unix commands
alias l="ls -lah"
alias grp="grep -rn"
alias *="sudo"
alias restartapache="sudo /usr/sbin/apachectl restart"

## Git
alias gst="git st"
alias gd="git d"
alias ga="git add"
alias gci="git commit"
alias gco="git checkout"
alias gcob="git checkout -b"
alias gl="git l"
alias glg="git lg"

## Drush
alias dcc="drush cc all"
alias dfu="drush fu -y"
alias dfr="drush fr -y"

## Other

# Generates a random string and copies it to the clipboard.
alias frand="cat /dev/urandom | env LC_CTYPE=C tr -cd 'a-f0-9' | head -c 16 | pbcopy"
